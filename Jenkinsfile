# from your local clone
git checkout main
# create Jenkinsfile
cat > Jenkinsfile <<'EOF'
# (paste Jenkinsfile content from below here)
EOF
git add Jenkinsfile
git commit -m "Add Jenkinsfile for CI/CD pipeline"
git push origin main
